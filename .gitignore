# Dependencies
node_modules/

# Build output
dist/

# Generated graph artifact (built offline by the build/ pipeline in V2)
public/graph/

# Offline pipeline data — raw OSM extract + heavy intermediates (regenerated by build/01,02).
# The scripts and PROVENANCE.md are tracked; the multi-hundred-MB binaries are not.
build/data/*
!build/data/PROVENANCE.md

# Staged build docs — local only, NEVER pushed (the code ships, the plans do not)
UPDATELOG*
