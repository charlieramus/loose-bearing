import { describe, it, expect } from "vitest";

// Placeholder suite so `npm test` is green on the fresh skeleton.
// Real geo/graph tests arrive in Stages 2–4.
describe("skeleton", () => {
  it("is wired up", () => {
    expect(true).toBe(true);
  });
});
