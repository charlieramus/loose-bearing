/// <reference types="vite/client" />
// Lets the app layer side-effect-import stylesheets (maplibre's CSS, our instrument CSS)
// under tsc's `moduleResolution: bundler` without a "cannot find module" error.
