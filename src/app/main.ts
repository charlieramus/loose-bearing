// App shell entry point. The real map UI arrives in V4; this is a placeholder mount.
const app = document.querySelector<HTMLDivElement>("#app");
if (app) {
  app.textContent = "Loose Bearing — foundations only (V1). Map UI arrives in V4.";
}
